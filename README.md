# crystalc225.github.io

my portfolio. lives at https://crystalc225.github.io

one `index.html`, no build step, no framework.

## turn on pages

repo Settings > Pages > Source > Deploy from a branch > `main` / root. live in a minute or two.

## still need to

- add dishcovery's "Source" link back once that repo's public again
- update skills list as it changes

## note

this repo can go private without breaking the live site - Pages still serves
publicly from a private repo, only the source hides. link the live URL
(not this repo) anywhere public, like a resume.

## adding a project

copy the `.project` block in `index.html` under `#projects`, before the "more projects" line.
